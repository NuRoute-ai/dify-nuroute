[project]
name = "dify-nuroute"
version = "0.1.0"
description = "NuRoute model provider plugin for Dify"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.10.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
